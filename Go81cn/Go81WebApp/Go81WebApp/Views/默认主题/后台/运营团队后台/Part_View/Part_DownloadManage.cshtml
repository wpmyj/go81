
@using Go81WebApp.Models.数据模型.内容数据模型;
<div class="title-base-out">
    <div class="title-base-middle">
        <div class="title-base-inner">下载列表</div>
    </div>
</div>
<div class="gys_znxx_box">
    <div class="gys_znxx_contentbox">
        <div class="gys_znxx_content">
            <table width="100%" cellpadding="0" cellspacing="0">
                <tr>
                    <th width="60%">标题</th>
                    <th width="10%">类型</th>
                    <th width="10%">验证码</th>
                    <th width="10%">时间</th>
                    <th width="10%">操作</th>
                </tr>
                @foreach (var item in (IEnumerable<下载>)ViewData["后台下载列表"])
                {
                    <tr>
                        <td><a href="~/运营团队后台/DownloadDetail?id=@item.Id" target="_blank">@item.内容主体.标题</a></td>
                        @if (item.下载类型 == 下载类型.普通)
                        {
                            <td>@item.下载类型</td>
                        }
                        else
                        {
                             <td style="color: red;">@item.下载类型</td>
                        }
                        <td>@item.下载验证码</td>
                        <td>@item.基本数据.修改时间.ToString("yyyy.MM.dd")</td>
                        <td class="gys_znxx_content_tabletime">@Html.ActionLink("修改", "DownloadModify", new { id = item.Id })|@Html.ActionLink("删除", "DownloadDelete", new { id = item.Id })</td>
                    </tr>
                }
            </table>
            <div class="gys_znxx_content_detailbox">
                @(Html.Pager("page", int.Parse(ViewData["pagesize"].ToString()), int.Parse(ViewData["listcount"].ToString())))
            </div>
        </div>
    </div>
</div>
<script type="text/javascript">
    $(function () {
        $("#下载管理").addClass("left_menu_select").children("a").css({ "color": "#fff" });
        $("#下载管理").parents().attr("show", "true").show().prev("li").addClass("open").children("span").css({ "background": "url('../Images/tree_icons.png') -115px -4px" });
    });
</script>


